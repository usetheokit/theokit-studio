# Release v0.1.0

**Date:** 2026-07-14
**Verdict:** RELEASED
**Source review:** none — inaugural **foundation release** (no `cycle-review`; this cut bootstraps `main`)
**PR:** none — bootstrap cut: `main` did not exist, so it was created from `develop` at the release commit (per CLAUDE.md Rule 4: "cut via merge develop→main + tag, manual or via PR"). Subsequent releases will use the `develop→main` PR flow.
**Release commit:** 43b40e3 (`chore(release): v0.1.0` on `develop`; `main` created pointing here)
**Tag:** v0.1.0 (annotated)
**GitHub release:** https://github.com/usetheodev/theokit-studio/releases/tag/v0.1.0

## Release notes

### Added
- Repository founded: README, CLAUDE.md contract, ROADMAP (M0–M4), architecture proposal and
  verified competitive deep-research under `docs/` (bootstrap, no issue ref yet)

## Cycle notes

- **Deviation from the standard chain (documented):** the standard `cycle-release` runs after
  `cycle-review` emits `READY_TO_MERGE` and opens a `develop→main` PR gated on human approval.
  This is the **inaugural** cut of a greenfield scaffold, so there was no upstream review and no
  pre-existing `main` to open a PR against. `main` was created directly from the release commit on
  `develop`. Rule 4 is preserved: nothing was committed *on* `main` — only the branch ref was created.
- **No ROADMAP checkbox flip:** this release has no plan / `milestone_id` (foundation, not a milestone).
  Per `cycle-release § Post-merge`, the flip is skipped with a WARN for ad-hoc/off-roadmap cuts.
  M0–M4 remain `[ ]`.
- **Next release:** now that `main` exists, cut future versions with the normal flow —
  `/review` → `/release`, which opens the `develop→main` PR and waits for approval.
